import 'package:flutter/material.dart';

class FashionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Layanan Fashion'),
      ),
      body: Center(
        child: Text('Ini adalah layar Fashion'),
      ),
    );
  }
}
